/**
 * @deprecated Use providers/AuthProvider instead.
 * This file is kept as an alias to avoid breaking existing imports.
 * Re-export from AuthProvider for backwards compatibility.
 */
export { useAuth } from "../providers/AuthProvider";
